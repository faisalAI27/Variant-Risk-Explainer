# mock, auto, or trained
MODEL_MODE=auto

# Path to a Hugging Face model exported from training/output/.../final_model
MODEL_DIR=

# Comma-separated origins for browser clients
ALLOWED_ORIGINS=http://localhost:3000

# Maximum sequence context length accepted by the API
MAX_SEQUENCE_CONTEXT_LENGTH=2000
